# Skill Radar Snapshot Template

Use this template for a new monthly or event snapshot such as `2026-05.md` or `2026-05-mid.md`.

---

# Skill Radar Snapshot: <YYYY-MM or YYYY-MM-label>

## Snapshot metadata

- reviewed on: `<YYYY-MM-DD>`
- review scope: `monthly` or `event`
- reviewer: `<name or agent>`
- previous snapshot: `<path>` or `none - baseline`

## Source set

List only the sources that actually informed this snapshot.

- `<source title>` — `<url>`
- `<source title>` — `<url>`

## What changed in the landscape

Name the shifts, not just the topics.

- `<shift 1>`
- `<shift 2>`

## Candidate patterns

For each pattern, separate the observed signal from the CraftKit judgment.

### Pattern: <short handle>

- observed signal:
- source support:
- likely benefit:
- likely risk:
- proposed status: `adopt` | `avoid` | `watch`
- CraftKit implication:

Repeat as needed.

## Adopt this cycle

- `<pattern>` — `<why it is now good enough to recommend>`

## Avoid this cycle

- `<pattern>` — `<why it is not a fit for CraftKit>`

## Watch, not default

- `<pattern>` — `<what still needs to become clearer>`

## Delta from previous snapshot

- promoted:
- demoted:
- unchanged but strengthened:
- unchanged but weakened:

## Proposed updates to current canonical view

- `current.md`:
- `decision-log.md`:

## Notes

Use for bounded context only. Do not turn this section into a generic research dump.
